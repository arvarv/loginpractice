defaults: &defaults
  adapter: postgresql
  encoding: utf8
  reconnect: false
  pool: 10
  username: user
  host: localhost
  database: app-dev
  
development:
  # <<: *defaults
  # database: app-dev
  # username: user
  # host: localhost

 adapter: postgresql
  host: localhost
  username: user
  database: app-dev

test:
  <<: *defaults
  database: cukcuk_testing

staging:
  <<: *defaults
  database: cukcuk_staging
  password: appstone

production:
  <<: *defaults
  database: cukcuk
  password: appstone
